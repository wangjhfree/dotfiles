# 解压缩 .xz 文件
xz 命令是压缩
  -z, --compress      强制压缩
  -d, --decompress    强制解压缩
  -t, --test          测试压缩文件的完整性
  -l, --list          列出压缩文件中的相关信息
  -k, --keep          压缩但不删除，输入文件
  -f, --force         解压强制覆盖文件
  -c, --stdout        标准输入，但不删除文件
  -0 .. -9            压缩等级;0-2快速压缩，3-5办好压缩，6-9优异的压缩，默认为6